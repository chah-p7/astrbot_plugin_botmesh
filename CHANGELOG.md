# Changelog

## 0.13.7

- 主动派发允许感受、观察、小事、见闻、感叹和联想，不再要求提问或用问句结尾。
- 放宽发送审核：模型未包 JSON 时，安全的自然纯文本会按全群消息保留，不再直接替换成通用问句。
- 点名识别只拦截 `@名字` 或“名字，/：”等明确收件人前缀；“莉芙的长头发……”一类叙事不再误判。
- 空消息、破损 JSON、未知目标、错误称呼、身份签名、平台路由和防回流校验继续保持严格。

## 0.13.2

- QQ 官方逻辑群跨 Bot 直连时不再拿发起 Bot 的 `group_openid` 为缺失地址的目标 Bot 兜底；群人格仍可留空继承全局，只有无法安全确定目标群地址时才拒绝发送。
- 管理页的选择器、关系标题和状态摘要优先显示 Bot 称呼与群名称，内部节点 ID 不再作为日常辨认信息。
- 统一管理页改为低饱和青绿与暖灰配色，并在群聊配置页显示每个逻辑群的映射完整度。

## 0.13.1

- 在两个插件管理页的业务脚本之前显式加载 AstrBot Plugin Page bridge SDK，修复页面初始化时 `AstrBotPluginPage` 尚未定义导致的 `apiGet` 报错。

## 0.13.0

- Persona 结构化身份新增可编辑 `memory_key`，用于表示主观记忆应跟随的稳定人物/意识，而不是平台账号。
- 人格编排 AI 和身份分栏 AI 可同时生成当前自我、灵魂/操控者、身体、身份说明、防历史覆盖策略与 `memory_key`，结果仍只写入草稿。
- 对外暴露实时记忆身份和受控写入接口，供 BotMesh Memory 在角色或灵魂跨账号移动时自动接回原有主观记忆。

## 0.12.0

- 将当前自我、灵魂/操控者、身体身份、身份说明和防历史覆盖开关并入 BotMesh 分群 Persona 配置。
- BotMesh Memory 不再维护独立身份副本，每轮生成均动态读取 BotMesh 当前配置；统一管理页保存后立即生效。
- 普通对话、直连 Agent、主动话题和多 Bot 会商共用同一身份解析结果，账号名和旧历史只作为路由与数据，不能覆盖开启保护的身份。

## 0.11.0

- 接入 BotMesh Memory：普通对话、独立 Agent、主动话题、受控回复和旁听插话统一写入分层记忆。
- 直连 Agent、主动话题和多 Bot 客观事实会商统一读取结构化记忆，缺少 Memory 插件时自动回退原有历史路径。
- Persona 生成链追加分群身份锁，明确区分平台账号、当前自我/灵魂和身体，避免灵魂互换设定被旧历史或账号名覆盖。

## 0.10.1

- 新增动态生活状态兼容接口，按真实平台账号解析当前 Bot、逻辑群和群专属 Persona。
- 接口一次返回同群关系相关 Bot 的有效 Persona 与有向关系，供动态生活状态插件联合生成客观一致的交叉日程。
- 灵魂互换等群 Persona 始终优先于平台账号静态名称，接口附带人格作用域与指纹供状态失效检测。

## 0.10.0

- 用户在同一条群消息中同时 @ 两个或更多 BotMesh Bot 时，所有参与 Bot 会先并行提交各自的客观事实清单，再由固定协调 Bot 生成一次共享事实表并注入各自最终回复。
- 只对齐人物身份、设定状态、已发生事件、时间线、地点、数字、术语、明确约束以及未知/冲突项；态度、喜恶、评价、情绪、建议、语气等主观内容继续由各 Bot 自己的人格和有向关系决定。
- 同一条消息在不同平台账号上的事件复用同一个会商任务；内部事实会商不发送到群聊，也不触发无关旁听 Bot。会商失败或超时会自动降级为原来的独立回复流程。
- 统一管理页新增启用开关、参与 Bot 上限、总超时和事实表 Token 预算；默认启用，最多 6 个 Bot 并行提交事实清单。

## 0.9.1

- 将人格、世界观和关系看法的 AI 分栏填写改为服务端后台任务，管理页只发起任务并短轮询状态，不再让浏览器长时间保持单个生成请求。
- 单次后台生成最长允许执行 5 分钟；状态轮询遇到短暂网络错误会自动重试，并原样显示 Provider 的真实失败原因。
- 保留原同步接口以兼容旧管理页，并限制同时排队的任务数量与完成结果保留时间。

## 0.9.0

- 将每个 Bot 的合并人格拆成“人格、身份与表达方式”和“世界观、经历与认知框架”，群专属字段可分别继承全局值，旧 `system_prompt` 自动兼容迁移。
- 为每条有向关系新增“发起方对目标的看法 / 认识”，并注入正常聊天、直连 Agent、主动话题、旁听与关系演化上下文。
- 统一管理页可按管理员要求，分别用 AI 生成人格、世界观或关系看法草稿；每次只改指定分栏，不授予权限，也不自动保存。
- 系统提示词默认自动附加“人格认知强化”和“自然人类表达 / 去 AI 化”规则；两段默认文本均可在统一管理页编辑或留空关闭。

## 0.8.18

- 主动话题只拦截正文开头由模型手写的对象称呼，叙事中合法出现 Persona 角色名不再整条降级为固定短句。
- AI 群人格改写在所选 Provider 超时或断网时自动尝试其余可用对话模型，并在草稿备注中标出实际备用模型。

## 0.8.17

- 修正 BotMesh 直连询问在群 Persona 与平台账号角色相反时被静态账号名覆盖的问题。
- 普通工具目录、目标 Agent、递归通信、旁听和关系演化统一把账号名仅视为路由标签；角色以有效群 Persona 为准。
- 直连回答按签名账号节点和当前方向群关系识别请求方，问题正文或历史里的误称不再覆盖身份。

## 0.8.16

- 将关系称呼扩展为后台单文本框维护的多值称呼库，首项为默认称呼。
- 动态关系可按上下文选择已有称呼，并把新形成的稳定称呼立即写入当前群称呼库。
- 主动派发允许模型从精确称呼白名单选择，发送层再次验证，禁止串用其他关系方向的称呼。
- 管理员删除候选或恢复默认时只清除动态称呼，不影响其他动态关系状态。
- 自动迁移旧数据库与单值 `address_as` 配置。

## 0.8.15

- 修正主动话题把平台账号静态名称误当作当前群角色身份的问题。
- 主动目标候选明确使用当前 Bot 指向目标 Bot 的群专属 `address_as`。
- 增加灵魂互换 Persona 与双向称呼覆盖的回归测试。

## 0.8.14

- 主动派发可从当前群全部出向 Bot 关系中选择精确目标，不再依赖最新一条签名 Bot 消息。
